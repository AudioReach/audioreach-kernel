name: Qualcomm Preflight Checks

on:
  pull_request_target:
    branches: [ kernel_patch ]
  push:
    branches: [ kernel_patch ]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  qcom-preflight-checks:
    uses: Audioreach/audioreach-workflows/.github/workflows/qcom-preflight-checks.yml@master
    secrets: inherit
